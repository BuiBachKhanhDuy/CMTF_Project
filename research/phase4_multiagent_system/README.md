# Phase 4 — Multi-Agent System

Status: **pending** — not yet written.

Planned scope:
- LangGraph agent topology (market / news / predict / research / rank / risk / gate / metalabel /
  critic / narrator / orchestrator) built on top of the frozen Phase 1–3 model artifacts
- Evaluation ladder and metrics vs. a base-LLM-only baseline
- Faithfulness / calibration / metalabel-gate results

Source material to draw from: `docs/reference/MULTIAGENT_SYSTEM.md`,
`docs/reference/MULTIAGENT_REDESIGN_PLAN.md`, `src/multiagent/`, `run_ab_benchmark.py`,
`results/agent_ablation/`.
